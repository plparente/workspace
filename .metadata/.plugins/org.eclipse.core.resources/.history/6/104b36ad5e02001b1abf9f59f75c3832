package decisao;

import javax.swing.JOptionPane;

public class DesafioDecisaoSimples {

	public static void main(String[] args) {
		/*
		 * Solicitar: nome e idade da pessoa e irão exibir:
		 * 
		 * "É obrigado a votar" => maior ou igual a 18 e menor que 70
		 * "Voto facultativo" => 16, 17 ou mais que 70
		 * "Não pode votar" => menores de 16 anos
		 * 
		 */

		String nome = JOptionPane.showInputDialog("Qual o nome?").toUpperCase();
		String idade = JOptionPane.showInputDialog("Qual a idade?").toUpperCase();
		
		System.out.println("nome + " + idade);
//		if (idade>=18 && idade<=70) {
//			System.out.println("nome " + " É obrigado a votar!!!");
//		}
		
		if (idade=16 || idade=17 || idade>=70) {
			System.out.println("nome " + "Voto facultativo!!!");
		
			if (idade<16) {
				System.out.println("nome " + "Não pode votar!!!");
			
			
			
		}
		
		
	}

}
